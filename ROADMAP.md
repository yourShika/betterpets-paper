Show rarity and default drop chance in the catalogue

Add clearer level milestone rewards

Add cosmetic unlocks at certain levels

Add safer handling for corrupted player pet data

Add optional database storage as a long-term goal

Add comments for every config option

Add automatic missing-option repair

Add config versioning

🧱 BetterModel Module

Add model validation warnings

Add fallback reason messages when a model cannot load
